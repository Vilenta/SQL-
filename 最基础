一、SELECT知识点——最基本
1、从一张表中获取数据：
SELECT...FROM...#从表格中取出某一列或某几列
SELECT* FROM...#从表格取出所有列的内容，用*表示
2、从两张表中获取不同的数据：【JOIN】
-- table_1中有id,age; table_2中有id，sex。想取出id,age,sex 三列信息
	-- 将table_1,table_2 根据主键id连接起来
	select a.id,a.age,b.sex from 
	(select id,age from table_1) a --将select之后的内容存为临时表a
	join[ inner join / left join /right join /full join ]
	(select id, sex from table_2) b --将select之后的内容存为临时表b
	on a.id =b.id
区别：
1.INNER JOIN ：左表，右表都包含的内容进行返回
2.LEFT JOIN：左表所有内容进行返回，即便右表中没有匹配
3.RIGHT JOIN：右表中所有内容进行返回，即便左表中没有匹配
4.FULL JOIN：左表+右表，所有内容进行返回，即使信息不全
注意：join 之前要确保关联键是否去重，是不是刻意保留非去重结果。
3、两张表格字段一样，进行数据合并【 UNION / UNION ALL 】
-- 不去重，合并两张表的数据
	select * from 
	(
	select id from table_1
	UNION ALL
	select id from table_2
	)t;
